services:
    - type: web
    name: pva-kronos-webhook
    env: python
    buildCommand: pip install -r requisitos.txt
    startCommand: python servidor_webhook.py
    envVars:
            - key: MP_ACCESS_TOKEN
        sync: false
            - key: MP_WEBHOOK_SECRET
        sync: false
